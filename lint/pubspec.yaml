name: fquery_lints
description: Custom lint rules for fquery package
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  # custom_lint dependencies
  custom_lint_builder: ^0.7.5
  analyzer: ^7.4.5
  analyzer_plugin: ^0.13.1
  
  # For accessing fquery types
  fquery:
    path: ../

dev_dependencies:
  test: ^1.25.15
  lints: ^4.0.0